mixin video()
  section.video(id="video")&attributes(attributes)
    .container
      .video__wrapper
        h2.video__title App Video Showcase
        video(width='770' height='443' loop muted playsinline)
          source(src='video/web.mp4' type='video/mp4')
